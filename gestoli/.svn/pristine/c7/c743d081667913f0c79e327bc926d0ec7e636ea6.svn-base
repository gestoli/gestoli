package es.caib.gestoli.front.spring.qualitat;

import org.apache.log4j.Logger;
import org.springframework.validation.Errors;
import org.springframework.validation.Validator;

public class QualitatProveidorRGSAValidator implements Validator {

	private static Logger logger = Logger.getLogger(QualitatProveidorRGSAValidator.class);
	
	public boolean supports(Class clazz) {
		return clazz.equals(QualitatProveidorRGSACommand.class);
	}
	
	public void validate(Object obj, Errors errors) {
	}

}
